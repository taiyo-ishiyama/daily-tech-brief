interface SourceBadgeProps {
  name: string;
  url?: string;
  className?: string;
}

export function SourceBadge({ name, url, className }: SourceBadgeProps) {
  void name;
  void url;
  void className;
  return null;
}
